version: 2

models:
  - name: Purchase_orders_input
    columns:
      - name: '"ID"'
        tests:
          - not_null
          - unique

  - name: Invoices_input
    columns:
      - name: '"ID"'
        tests:
          - not_null
          - unique
