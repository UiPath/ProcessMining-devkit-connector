devkit_connector:
  outputs:

    sqlserver:
      type: sqlserver
      driver: 'ODBC Driver 17 for SQL Server'
      server: 
      port: 1433
      user:
      password:
      database:
      schema: "devkit_connector"

    snowflake:
      type: snowflake
      account:
      user:
      password:
      role:
      database:
      warehouse:
      schema: "devkit_connector"

  target: sqlserver
